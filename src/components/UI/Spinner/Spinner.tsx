import {CircularProgress} from "@mui/material";

const Spinner = () => {
    return (
        <div>
            <CircularProgress sx={{color: '#171717'}}/>
        </div>
    );
};

export default Spinner;